# Database
DATABASE_URL="postgresql://user:password@localhost:5432/lucrofy"

# Auth
JWT_SECRET="your-super-secret-jwt-key-here"
PASSWORD_SALT="your-password-salt-here"

# Meta Ads (configured per account in the app)
# No global env needed - tokens are stored per account in the database
